base=float(input("qual é a base do retângulo? "))
while base<=0:
    base = float(input("não pode ser menor que 1: "))
altura=float(input("qual é a altura do retângulo? "))
while altura<=0:
    altura = float(input("não pode ser menor que 1: "))
area=base*altura
print(area)
